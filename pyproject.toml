[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "traitprint"
version = "0.8.0"
description = "A structured career profile that AI tools can query. Local-first vault with MCP server."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "DataViking Technologies", email = "hello@traitprint.com" },
]
keywords = ["mcp", "career", "identity", "vault", "local-first", "ai-agents"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "click>=8.0",
    "pydantic>=2.0",
    "mcp",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
cloud = [
    "httpx",
]
import = [
    "pypdf>=4.0",
    "python-docx>=1.0",
    "httpx",
]
dev = [
    "pytest",
    "ruff",
    "mypy", "types-PyYAML",
    "httpx",
]

[project.scripts]
traitprint = "traitprint.cli:cli"

[project.urls]
Homepage = "https://traitprint.com"
Repository = "https://github.com/DataViking-Tech/traitprint"
Issues = "https://github.com/DataViking-Tech/traitprint/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/traitprint"]

# The canonical Agent Skills live at the repo root (`skills/`) so that
# `npx skills add DataViking-Tech/traitprint` can consume them. Wheels
# repackage the same directory as package data; `traitprint.skills`
# resolves the package-data copy first and falls back to the repo root
# for source checkouts / editable installs.
[tool.hatch.build.targets.wheel.force-include]
"skills" = "traitprint/data/skills"

[tool.ruff]
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
